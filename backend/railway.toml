# Keep this minimal. Railpack uses package.json `build` / `railway:start`
# and resolves Node from engines.node + .nvmrc (Node 24).
[deploy]
startCommand = "npm run railway:start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
