# Used when the service Config-as-Code path is set to /backend/railway.toml.
[build]
builder = "RAILPACK"

[deploy]
startCommand = "npm run railway:start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
