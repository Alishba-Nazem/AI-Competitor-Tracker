# Used when Config-as-Code path is /backend/railway.toml.
[build]
builder = "RAILPACK"

[deploy]
preDeployCommand = "npm run railway:predeploy"
startCommand = "npm run railway:start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
