# Copy to .env and fill in local values. Never commit real secrets.

DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"
DIRECT_DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# Railway sets PORT automatically. For local:
PORT=3000

# Comma-separated allowed frontend origins (your Vercel URL)
FRONTEND_URL="http://localhost:3001"

# Skip downloading Playwright browsers during install (optional; reviews may need browsers later)
# PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
