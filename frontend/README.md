# Frontend

Next.js UI for Ecommerce Competitor Tracker.

Local setup, architecture, AI notes, and deploy are in the [root README](../README.md).

```bash
npm install
# .env.local: NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
npm run dev    # http://localhost:3001
npm test
```
